<Window x:Class="PIDSimulatorGip.view.MainSimulationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PIDSimulatorGip.view"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="MainSimulationWindow" MinHeight="700" MinWidth="1200">
    <Grid Background="SlateGray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!--#region side bar region-->
        <Grid x:Name="SidebarGrid" Grid.RowSpan="2" Background="DimGray" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Margin="10" Height="20" Width="20" Background="RoyalBlue"/>
            <Button Grid.Row="1"  Height="20" Width="20" Background="RosyBrown" Command="{Binding SerialCommCommand}"/>
            <Button Grid.Row="2"  Height="20" Width="20" Background="DarkOliveGreen"/>
            <Button Grid.Row="3"  Height="20" Width="20" Background="White"/>
        </Grid>
        <!--#endregion-->
        <!--#region PIDControlGrid-->
        <Grid Grid.Column="1" Name="PIDControlGrid" Margin="15" HorizontalAlignment="Left" Visibility="Visible">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>

            </Grid.RowDefinitions>

            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="type" FontSize="15" Foreground="White"/>
            <ComboBox Grid.Column="1" HorizontalAlignment="Center" Name="TypeCMB" IsEnabled="{Binding IsRunning}" Height="20" Width="150" SelectedValue="{Binding Type, Mode=TwoWay}">
                <ComboBoxItem Content="Type A"/>
                <ComboBoxItem Content="Type B"/>
                <ComboBoxItem Content="Type C"/>
            </ComboBox>

            <TextBlock Grid.Row="1" Text="Wenswaarde  " FontSize="15" Foreground="White"/>
            <Slider Name="WensWaardeSlider" Grid.Row="1" Grid.Column="1" Minimum="0" Maximum="100" Width="200" Value="{Binding W, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
            <TextBlock x:Name="WensWaardeTB" Grid.Row="1" Grid.Column="2" FontSize="15" Foreground="White" Text="{Binding W, UpdateSourceTrigger=PropertyChanged, StringFormat=F2, Mode=TwoWay}"/>

            <TextBlock Grid.Row="2" Text="P-actie  " FontSize="15" Foreground="White"/>
            <Slider Name="PactieSlider" Grid.Row="2" Grid.Column="1" Minimum="0" Maximum="5" Width="200" Value="{Binding VSFP, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
            <TextBlock Name="PactieTB" Grid.Row="2" Grid.Column="2" FontSize="15" Foreground="White" Text="{Binding VSFP, UpdateSourceTrigger=PropertyChanged, StringFormat=F3, Mode=TwoWay}"/>

            <TextBlock Grid.Row="3" Text="I-actie  " FontSize="15" Foreground="White"/>
            <Slider Name="IactieSlider" Grid.Row="3" Grid.Column="1" Minimum="0" Maximum="1" Width="200" Value="{Binding VSFI, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
            <TextBlock Name="IactieTB" Grid.Row="3" Grid.Column="2" FontSize="15" Foreground="White" Text="{Binding VSFI, UpdateSourceTrigger=PropertyChanged, StringFormat=F3, Mode=TwoWay}"/>

            <TextBlock Grid.Row="4" Text="D-actie  " FontSize="15" Foreground="White"/>
            <Slider Name="DactieSlider" Grid.Row="4" Grid.Column="1" Minimum="0" Maximum="5" Width="200" Value="{Binding VSFD, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
            <TextBlock Name="DactieTB" Grid.Row="4" Grid.Column="2" FontSize="15" Foreground="White"  Text="{Binding VSFD, UpdateSourceTrigger=PropertyChanged, StringFormat=F3, Mode=TwoWay}"/>


        </Grid>
        <!--#endregion-->
        <!--#region proces grid -->
        <Grid x:Name="ProcesGrid" Grid.Row="1" Grid.Column="1" Margin="10" HorizontalAlignment="Left" Visibility="Visible">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Text="ProcesKracht   " FontSize="15" Foreground="White"/>
            <Slider Grid.Column="1" Name="ProcesKrachtSlider" Grid.ColumnSpan="2" Minimum="0" Maximum="5" Width="200" IsEnabled="{Binding IsRunning}" Value="{Binding Kracht, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
            <TextBlock Grid.Column="3" FontSize="15" Foreground="White" Text="{Binding Kracht, UpdateSourceTrigger=PropertyChanged, StringFormat=F2, Mode=TwoWay}"/>

            <TextBlock Grid.Row="1" Text="proces orde" FontSize="15" Foreground="White"/>
            <ComboBox Name="ProcesOrdeCMB" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Height="20" Width="100" HorizontalAlignment="Left" VerticalAlignment="Top" IsEnabled="{Binding IsRunning}" SelectedValue="{Binding Orde, Mode=TwoWay}">
                <ComboBoxItem Content="0orde"/>
                <ComboBoxItem Content="1orde"/>
                <ComboBoxItem Content="2orde"/>
            </ComboBox>

            <TextBlock Text="dodetijd" FontSize="15" Foreground="White" Grid.Row="2"/>
            <ComboBox Name="DodetijdCMB" Height="20" Width="150" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Left" VerticalAlignment="Top" IsEnabled="{Binding IsRunning}" SelectedValue="{Binding DodeTijd, Mode=TwoWay}">
                <ComboBoxItem Content="geen dodetijd"/>
                <ComboBoxItem Content="klein beetje dodetijd"/>
                <ComboBoxItem Content="wat dodetijd"/>
                <ComboBoxItem Content="gemiddeld dodetijd"/>
                <ComboBoxItem Content="meer dan gemiddeld dodetijd"/>
                <ComboBoxItem Content="veel dodetijd"/>
            </ComboBox>

            <TextBlock Grid.Row="3" Text="tijdsconstante" FontSize="15" Foreground="White"/>
            <Slider Name="TijdsConstanteSlider" Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2"  Minimum="0" Maximum="5" Width="200" IsEnabled="{Binding IsRunning}" Value="{Binding TijdsConstante, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Name="TijdsConstanteTB" Grid.Row="3" Grid.Column="3" FontSize="15" Foreground="White"  HorizontalAlignment="Right" Text="{Binding TijdsConstante, UpdateSourceTrigger=PropertyChanged, StringFormat=F2, Mode=TwoWay}"/>

            <TextBlock Grid.Row="4" FontSize="15" Foreground="White" Text="simulatie snelheid  "/>
            <Slider Name="SimulatieSnelheidSlider" Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Minimum="0.5" Maximum="10" IsEnabled="{Binding IsRunning}" Value="{Binding SimulatieSnelheid,  UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Name="SimulatieSnelheidTB" Grid.Row="4" Grid.Column="3" FontSize="15" Foreground="White" Text="{Binding SimulatieSnelheid, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"/>


            <Button x:Name="StartBtn" Grid.Row="5" Content="Start" Height="20" Width="50" Command="{Binding StartCommand}"/>
            <Button Name="StopBtn" Grid.Row="5" Grid.Column="2" Content="reset" Height="20" Width="50" Command="{Binding ResetCommand}"/>
            <Button Name="PauseBtn" Grid.Row="5" Grid.Column="1" Content="Pauze" Height="20" Width="50" HorizontalAlignment="Center" Command="{Binding PauzeCommand}"/>
        </Grid>
        <!--#endregion-->
        <!--#region serial connection grid -->
        <StackPanel Name="SerialStackPanel" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" Visibility="Hidden">
            <TextBlock Text="SerialComunication" FontSize="15" Foreground="White" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            <WrapPanel HorizontalAlignment="Center">
                <TextBlock Text="auto connect" FontSize="15" Foreground="White"/>
                <CheckBox Name="AutoSerialCKB" IsChecked="True" Margin="5,4"/>
            </WrapPanel>
            <ComboBox Name="SerialSelectionCMB" />
            <WrapPanel>
                <Button Content="connect" Name="SerialConnectBtn" Height="20" Width="60" Margin="10"/>
                <Button Content="disconnect" Name="SerialDisconectBtn" Height="20" Width="60" Margin="10"/>
            </WrapPanel>
        </StackPanel>
        <!--#endregion-->
        <!--#region graph -->
        <Grid Name="GraphGrid" Grid.Row="1" Grid.Column="2" Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <oxy:PlotView Model="{Binding MyPlot}" />
        </Grid>
        <!--#endregion-->
        <!--#region animation-->
        <Grid Name="AnimationGrid" Grid.Column="2" Margin="10"
      VerticalAlignment="Stretch" HorizontalAlignment="Stretch">

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Centered WaterTankAnimation -->
            <Grid Name="WaterTankAnimation" Grid.Row="1" Grid.Column="1"
          VerticalAlignment="Center" HorizontalAlignment="Center"
          Visibility="Visible">

                <!-- Water Tank Container -->
                <Border BorderBrush="Black" BorderThickness="2" CornerRadius="5"
                Width="300" Height="300" Background="LightGray"
                VerticalAlignment="Center" HorizontalAlignment="Center">

                    <!-- Water Level -->
                    <Grid VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Margin="5">
                        <Rectangle Name="WaterRectangle" Fill="LightBlue"
                           VerticalAlignment="Bottom" HorizontalAlignment="Stretch"
                           Height="{Binding ProcesWaarde}"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!--#endregion-->
    </Grid>
</Window>
